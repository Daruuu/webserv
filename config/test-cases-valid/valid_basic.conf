server {
    listen 8080;
    server_name example.com;
    root /var/www/html;
    index index.html;

    location / {
        limit_except GET POST;
    }

    location /uploads {
        upload_store /var/www/uploads;
    }
}
